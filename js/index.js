"use strict";


function init() {
    
/*     const filtered = cells.filter((field) => 
    (
    Math.abs(field.x - this.x) <= 1 &&
    Math.abs(field.y - this.y) <= 1
    ) &&
    !(
    field.x == this.x && 
    field.y == this.y 
    ) &&
    field.state == 1 
    ); */
}


init();



